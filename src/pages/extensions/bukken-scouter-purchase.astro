---
import Layout from '../../layouts/Layout.astro';

const extension = {
  name: "物件スカウター（購入版）",
  subtitle: "不動産購入の意思決定をデータでサポート",
  tagline: "坪単価がわかれば、物件選びが変わる。",
  price: "Free",
  status: "Available",
  chromeUrl: "https://chromewebstore.google.com/detail/ioboepegcabodocflleblblnknlfpjka",
  category: "Real Estate",
  lang: "JA",
  icon: "/icons/bukken-scouter-purchase.png",
  rating: 0,
  reviewCount: 0,
};

const overview = "物件スカウター（購入版）は、SUUMOの購入物件ページで坪単価・㎡単価を自動計算し、物件カードに表示するChrome拡張機能です。数千万円の買い物だからこそ、感覚ではなくデータで判断したい。そんな方のために、築年数の色分けハイライト、お気に入り保存、比較表、価格履歴トラッキングなど、効率的な物件探しをサポートする機能を搭載しました。完全無料で利用でき、データは全てブラウザ内にローカル保存。プライバシーも安心です。";

const features = [
  { icon: "📊", title: "坪単価・㎡単価の自動計算", desc: "物件カードに自動表示、一目でコスパを把握" },
  { icon: "🏠", title: "築年数ハイライト", desc: "10年以内（緑）、20年以内（黄）、30年超（赤）で色分け" },
  { icon: "⭐", title: "お気に入り保存", desc: "気になる物件を無制限に保存" },
  { icon: "📋", title: "比較表", desc: "複数物件を並べて検討" },
  { icon: "📈", title: "価格履歴トラッキング", desc: "物件の価格変動を記録" },
  { icon: "📁", title: "CSV/Excelエクスポート", desc: "データを出力して家族と共有" },
  { icon: "🗺️", title: "ハザードマップ連携", desc: "ワンクリックで災害リスクを確認" },
  { icon: "🔒", title: "完全ローカル保存", desc: "データは外部送信なし、プライバシー安心" },
];

const supportedSites = ["SUUMO（スーモ）購入物件ページ"];

const targetUsers = [
  "中古マンション・戸建ての購入を検討中の方",
  "坪単価で複数物件を比較したい方",
  "割高な物件を避けたい方",
  "物件情報を整理して家族と共有したい方",
  "効率的に物件探しを進めたい方",
];

const screenshots = [
  "/screenshots/bukken-scouter-purchase/screenshot-1.png",
  "/screenshots/bukken-scouter-purchase/screenshot-2.png",
  "/screenshots/bukken-scouter-purchase/screenshot-3.png",
];

const relatedProducts = [
  { name: "物件スカウター（賃貸版）", category: "Productivity", link: "/extensions/bukken-scouter-rental", icon: "/icons/bukken-scouter-rental.png" },
  { name: "DataPick", category: "Productivity", link: "/extensions/datapick", icon: "/icons/datapick.png" },
];

const softwareSchema = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "物件スカウター（購入版）",
  "description": extension.tagline,
  "applicationCategory": "BrowserApplication",
  "operatingSystem": "Chrome",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "JPY"
  },
  "author": {
    "@type": "Organization",
    "name": "S-Hub",
    "url": "https://dev-tools-hub.xyz"
  },
  "downloadUrl": extension.chromeUrl,
};
---

<Layout
  title={`${extension.name} - SUUMO購入物件の坪単価計算ツール`}
  description="SUUMOの購入物件で坪単価・㎡単価を自動計算。築年数ハイライト、お気に入り比較、価格履歴で賢い物件探しをサポート。"
  keywords="SUUMO, 購入, 物件比較, Chrome拡張機能, 坪単価, ㎡単価, 物件スカウター, 不動産"
  ogImage="/screenshots/bukken-scouter-purchase/screenshot-1.png"
>
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify(softwareSchema)} />
  </Fragment>

  <div class="pt-24 pb-16">
    <!-- Hero Section with CTA -->
    <section class="px-6 py-12 max-w-4xl mx-auto">
      <a href="/" class="inline-flex items-center text-zinc-400 hover:text-white mb-8 transition-colors">
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        Back to Home
      </a>

      <div class="flex flex-col md:flex-row gap-8 items-start">
        <img
          src={extension.icon}
          alt={`${extension.name} icon`}
          class="w-24 h-24 rounded-2xl bg-zinc-800 shadow-lg"
        />
        <div class="flex-1">
          <div class="flex flex-wrap items-center gap-3 mb-4">
            <span class="text-xs font-bold text-blue-400 px-3 py-1 bg-blue-500/10 rounded-full border border-blue-500/20 uppercase tracking-wider">
              {extension.category}
            </span>
            <span class="text-xs font-bold text-green-400 px-3 py-1 bg-green-500/10 rounded-full border border-green-500/20 uppercase tracking-wider">
              {extension.status}
            </span>
            <span class="text-xs font-bold text-orange-400 px-3 py-1 bg-orange-500/10 rounded-full border border-orange-500/20 uppercase tracking-wider">
              {extension.lang}
            </span>
          </div>
          <h1 class="text-4xl md:text-5xl font-bold text-white mb-2">
            {extension.name}
          </h1>
          <p class="text-xl text-zinc-400 mb-4">{extension.subtitle}</p>

          <div class="flex items-center gap-4 mb-6">
            <span class="price-badge-free">{extension.price}</span>
            {extension.rating > 0 && (
              <div class="rating-badge">
                <svg class="w-4 h-4 fill-yellow-500" viewBox="0 0 24 24">
                  <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                </svg>
                <span>{extension.rating.toFixed(1)} ({extension.reviewCount} reviews)</span>
              </div>
            )}
          </div>

          <p class="text-zinc-300 mb-8">{extension.tagline}</p>

          <!-- CTA Buttons at Top -->
          <div class="flex flex-wrap gap-4">
            <a href={extension.chromeUrl} target="_blank" rel="noopener noreferrer" class="btn-blue flex items-center gap-2 px-6 py-3">
              <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 0C8.21 0 4.831 1.757 2.632 4.501l3.953 6.848A5.454 5.454 0 0 1 12 6.545h10.691A12 12 0 0 0 12 0zM1.931 5.47A11.943 11.943 0 0 0 0 12c0 6.012 4.42 10.991 10.189 11.864l3.953-6.847a5.45 5.45 0 0 1-6.865-2.29zm13.342 2.166a5.446 5.446 0 0 1 1.45 7.09l.002.001h-.002l-3.952 6.848a12.014 12.014 0 0 0 9.229-9.605zM12 16.364a4.364 4.364 0 1 1 0-8.728 4.364 4.364 0 0 1 0 8.728z"/>
              </svg>
              無料でインストール
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Screenshots Carousel -->
    {screenshots.length > 0 && (
      <section class="px-6 py-12 max-w-4xl mx-auto">
        <h2 class="text-2xl font-bold text-white mb-6">Screenshots</h2>
        <div class="relative">
          <div class="overflow-hidden rounded-xl border border-zinc-800">
            <div id="carousel" class="flex transition-transform duration-300 ease-in-out">
              {screenshots.map((src, i) => (
                <div class="w-full flex-shrink-0">
                  <img
                    src={src}
                    alt={`${extension.name} screenshot ${i + 1}`}
                    class="w-full"
                    loading="lazy"
                  />
                </div>
              ))}
            </div>
          </div>

          {screenshots.length > 1 && (
            <>
              <button
                id="prev-btn"
                class="absolute left-2 top-1/2 -translate-y-1/2 w-10 h-10 bg-black/60 hover:bg-black/80 rounded-full flex items-center justify-center text-white transition-colors"
                aria-label="Previous screenshot"
              >
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                </svg>
              </button>
              <button
                id="next-btn"
                class="absolute right-2 top-1/2 -translate-y-1/2 w-10 h-10 bg-black/60 hover:bg-black/80 rounded-full flex items-center justify-center text-white transition-colors"
                aria-label="Next screenshot"
              >
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                </svg>
              </button>

              <div class="flex justify-center gap-2 mt-4">
                {screenshots.map((_, i) => (
                  <button
                    class="carousel-dot w-2 h-2 rounded-full bg-zinc-600 transition-colors"
                    data-index={i}
                    aria-label={`Go to screenshot ${i + 1}`}
                  />
                ))}
              </div>
            </>
          )}
        </div>
      </section>
    )}

    <script>
      const carousel = document.getElementById('carousel');
      const prevBtn = document.getElementById('prev-btn');
      const nextBtn = document.getElementById('next-btn');
      const dots = document.querySelectorAll('.carousel-dot');
      const totalSlides = dots.length;
      let currentIndex = 0;

      function updateCarousel() {
        if (carousel) {
          carousel.style.transform = `translateX(-${currentIndex * 100}%)`;
        }
        dots.forEach((dot, i) => {
          dot.classList.toggle('bg-blue-500', i === currentIndex);
          dot.classList.toggle('bg-zinc-600', i !== currentIndex);
        });
      }

      prevBtn?.addEventListener('click', () => {
        currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
        updateCarousel();
      });

      nextBtn?.addEventListener('click', () => {
        currentIndex = (currentIndex + 1) % totalSlides;
        updateCarousel();
      });

      dots.forEach((dot, i) => {
        dot.addEventListener('click', () => {
          currentIndex = i;
          updateCarousel();
        });
      });

      updateCarousel();
    </script>

    <!-- Overview -->
    <section class="px-6 py-12 max-w-4xl mx-auto">
      <h2 class="text-2xl font-bold text-white mb-6">Overview</h2>
      <p class="text-zinc-400 text-lg leading-relaxed">
        {overview}
      </p>
    </section>

    <!-- Supported Sites -->
    <section class="px-6 py-12 max-w-4xl mx-auto">
      <h2 class="text-2xl font-bold text-white mb-6">対応サイト</h2>
      <div class="flex flex-wrap gap-4">
        {supportedSites.map((site) => (
          <div class="bg-zinc-900/50 border border-zinc-800 rounded-xl px-6 py-3 text-white font-semibold">
            {site}
          </div>
        ))}
      </div>
    </section>

    <!-- Features -->
    <section class="px-6 py-12 max-w-4xl mx-auto">
      <h2 class="text-2xl font-bold text-white mb-6">Features</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {features.map((feature) => (
          <div class="bg-zinc-900/50 border border-zinc-800 rounded-xl p-6">
            <div class="flex items-start gap-4">
              <span class="text-2xl">{feature.icon}</span>
              <div>
                <h3 class="font-semibold text-white mb-1">{feature.title}</h3>
                <p class="text-zinc-400 text-sm">{feature.desc}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- Recommended For -->
    <section class="px-6 py-12 max-w-4xl mx-auto">
      <h2 class="text-2xl font-bold text-white mb-6">こんな方におすすめ</h2>
      <ul class="space-y-4">
        {targetUsers.map((user) => (
          <li class="flex items-start gap-4 text-zinc-400">
            <svg class="w-5 h-5 text-blue-500 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
            <span>{user}</span>
          </li>
        ))}
      </ul>
    </section>

    <!-- Install Section (Main CTA) -->
    <section class="px-6 py-12 max-w-4xl mx-auto">
      <div class="bg-gradient-to-r from-blue-600/20 to-purple-600/20 border border-zinc-800 rounded-2xl p-8 text-center">
        <h2 class="text-2xl font-bold text-white mb-4">データで選ぶ、賢い物件探し</h2>
        <p class="text-zinc-400 mb-8">物件スカウターで、坪単価を見ながら効率的に物件を比較しましょう。</p>
        <div class="flex flex-wrap justify-center gap-4">
          <a href={extension.chromeUrl} target="_blank" rel="noopener noreferrer" class="btn-blue flex items-center gap-2 px-8 py-4 text-lg">
            <svg class="w-6 h-6" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0C8.21 0 4.831 1.757 2.632 4.501l3.953 6.848A5.454 5.454 0 0 1 12 6.545h10.691A12 12 0 0 0 12 0zM1.931 5.47A11.943 11.943 0 0 0 0 12c0 6.012 4.42 10.991 10.189 11.864l3.953-6.847a5.45 5.45 0 0 1-6.865-2.29zm13.342 2.166a5.446 5.446 0 0 1 1.45 7.09l.002.001h-.002l-3.952 6.848a12.014 12.014 0 0 0 9.229-9.605zM12 16.364a4.364 4.364 0 1 1 0-8.728 4.364 4.364 0 0 1 0 8.728z"/>
            </svg>
            無料でインストール
          </a>
        </div>
      </div>
    </section>

    <!-- Related Extensions -->
    <section class="px-6 py-12 max-w-4xl mx-auto">
      <h2 class="text-2xl font-bold text-white mb-6">You might also like</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
        {relatedProducts.map((product) => (
          <a href={product.link} class="product-card group p-6 flex items-center gap-4">
            <img src={product.icon} alt={product.name} class="w-12 h-12 rounded-xl bg-zinc-800" />
            <div>
              <h3 class="font-semibold text-white group-hover:text-blue-400 transition-colors">{product.name}</h3>
              <p class="text-xs text-zinc-400 uppercase tracking-wider">{product.category}</p>
            </div>
            <svg class="w-5 h-5 text-zinc-400 ml-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
            </svg>
          </a>
        ))}
      </div>
    </section>

    <!-- Disclaimer -->
    <section class="px-6 py-8 max-w-4xl mx-auto">
      <div class="text-zinc-500 text-sm space-y-1">
        <p>※ SUUMOは株式会社リクルートの登録商標です</p>
        <p>※ 本ツールは株式会社リクルートとは関係のない個人開発です</p>
        <p>※ 表示される情報は参考値です</p>
      </div>
    </section>
  </div>
</Layout>
